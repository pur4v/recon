<!-- FICTIONAL worked example. See ../../README.md. Copy inventory (invented strings). -->

# Copy inventory: Nimbus Studio

- **Captured:** 2026-02-01 · Source: rendered DOM (`screens.json` → `copy`) + screenshots.

## Voice & tone (observed)

Warm, second-person, action-first. Sentence case. No emoji. E.g. `Run your first render`,
`Upgrade to keep creating`.

## Per-screen copy

### Create — `/create`

| Element | Exact text | Notes |
|---|---|---|
| Page title | `Create · Nimbus Studio` | |
| Primary CTA | `New render` | opens the tool form |
| Spark balance | `482 sparks` | → `/billing` |
| Empty state | `Run your first render` | shown with no history |
| Loading | `Rendering…` | skeleton cards |
| Paywall CTA | `Upgrade to keep creating` | shown at 0 sparks |
| Success toast | `Render ready` | on WS complete |

## Global / shell copy

Nav: `Create` · `Brand Kit` · `Library` · `Campaigns`. Account menu: `Settings`, `Billing`,
`Sign out`. Consent banner: `We use cookies to improve Nimbus.`

## Unknown / not observed

- Server-error banner copy not observed (couldn't safely force a 500).
