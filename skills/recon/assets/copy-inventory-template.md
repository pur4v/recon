<!--
  recon COPY INVENTORY — the exact words. You can't rebuild a product without its microcopy.
  Captured from the rendered DOM (capture.mjs writes rec.copy per screen). Transcribe
  VERBATIM — this is the product's own text, quoted for a rebuild, not paraphrased.
  Save to .recon/dossier/copy-inventory.md. Rule: every string carries the screen it's from.
-->

# Copy inventory: <Product>

- **Captured:** <YYYY-MM-DD> · Source: rendered DOM (`.recon/screens.json` → `copy`) + screenshots.

## Voice & tone (observed)

<2–3 lines: is it formal/playful, terse/verbose, sentence/Title Case, emoji, person ("you")?
Evidence: a couple of quoted strings that show it.>

## Per-screen copy

### <Screen name> — `/route`

| Element | Exact text | Notes |
|---|---|---|
| Page title | `<verbatim>` | |
| Heading | `<verbatim>` | |
| Primary CTA | `<verbatim>` | → <where it goes> |
| Secondary CTA | `<verbatim>` | |
| Field label / placeholder | `<verbatim>` | |
| Empty state | `<verbatim>` | shown when <condition> |
| Loading | `<verbatim>` | |
| Error / validation | `<verbatim>` | trigger: <what caused it> |
| Success / toast | `<verbatim>` | |
| Helper / tooltip | `<verbatim>` | |

*(Repeat per screen. Mark any state "not observed" rather than inventing the wording.)*

## Global / shell copy

<Nav labels, footer, cookie/consent banner, account menu, upgrade prompts — the strings that
repeat across screens. Quote verbatim.>

## Unknown / not observed

- <states whose copy you couldn't trigger with this account>
